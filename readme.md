# Proyecto Básico Flask con Docker

Este proyecto demuestra cómo montar una aplicación básica de Flask dentro de un contenedor Docker.

## Pasos para ejecutar

1. Construir la imagen:
   ```bash
   docker-compose build
